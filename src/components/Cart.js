import React from 'react';

function Cart(props) {
    return (
        <div className="cart">
            <button className="close" onClick={props.close}>X</button>
            <ul>
                {

                }
            </ul>
        </div>
    )
}

export default Cart;
